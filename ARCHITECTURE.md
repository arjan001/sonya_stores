# Sonya Stores - System Architecture Overview

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            SONYA STORES PLATFORM                            │
└─────────────────────────────────────────────────────────────────────────────┘

                           ┌──────────────────────┐
                           │   CLIENT BROWSERS    │
                           └──────────┬───────────┘
                                      │
                    ┌─────────────────┴────────────────┐
                    │                                  │
         ┌──────────▼──────────┐         ┌────────────▼────────────┐
         │  CUSTOMER FRONTEND  │         │   ADMIN PANEL           │
         │                     │         │                         │
         │ - Homepage          │         │ - Auth (Register/Login) │
         │ - Shop/Products     │         │ - Dashboard             │
         │ - Cart              │         │ - Settings CRUD         │
         │ - Checkout          │         │ - Products CRUD (Soon)  │
         │ - Orders            │         │ - Orders CRUD (Soon)    │
         │ - Track Orders      │         │ - Customers (Soon)      │
         └──────────┬──────────┘         └────────────┬────────────┘
                    │                                  │
                    └──────────────────┬───────────────┘
                                       │
                          ┌────────────▼────────────┐
                          │   NEXT.JS 16 SERVER    │
                          │   Node.js API Routes   │
                          └────────────┬────────────┘
                                       │
         ┌─────────────────────────────┼─────────────────────────────┐
         │                             │                             │
    ┌────▼─────┐  ┌────────────┐  ┌───▼────┐  ┌──────────────┐  ┌───▼────┐
    │  API     │  │  AUTH      │  │ SETTINGS│  │  PRODUCTS    │  │ ORDERS │
    │ ROUTES   │  │ ENDPOINTS  │  │ ENDPOINT│  │ ENDPOINTS    │  │ENDPOINT│
    └────┬─────┘  └────────────┘  └───┬────┘  └──────────────┘  └───┬────┘
         │                            │                             │
         └────────────────┬───────────┴─────────────────┬───────────┘
                          │                             │
                          │    BCRYPT PASSWORD          │
                          │    HASHING & TOKENS         │
                          │                             │
                 ┌────────▼─────────────────────────┐
                 │  NEON POSTGRESQL DATABASE        │
                 │  (Connection: DATABASE_URL)      │
                 └────────┬─────────────────────────┘
                          │
         ┌────────────────┼────────────────┐
         │                │                │
    ┌────▼──────┐  ┌──────▼──────┐  ┌─────▼─────┐
    │  TABLES   │  │  INDEXES    │  │ TRIGGERS  │
    │  (9 total)│  │  (optimized)│  │  (auto    │
    │           │  │             │  │   updated)│
    └───────────┘  └─────────────┘  └───────────┘
```

## 🔄 Data Flow Diagrams

### Admin Registration Flow
```
User @ /admin/register
         │
         ▼
┌─────────────────────────────┐
│  Fill Registration Form     │
│  - Email                    │
│  - Name                     │
│  - Password                 │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  Client Validation          │
│  - Email format             │
│  - Passwords match          │
│  - Min 6 chars              │
└────────────┬────────────────┘
             │
             ▼
  POST /api/admin/register
             │
             ▼
┌─────────────────────────────┐
│  Server Validation          │
│  - Check email unique       │
│  - Validate input           │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  Hash Password              │
│  bcrypt(password, 10)       │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  Determine Role             │
│  - Count admins             │
│  - First = super_admin      │
│  - Others = admin           │
└────────────┬────────────────┘
             │
             ▼
┌─────────────────────────────┐
│  Save to Database           │
│  INSERT INTO admins         │
└────────────┬────────────────┘
             │
             ▼
Redirect to /admin/login ✅
```

### Admin Login Flow
```
User @ /admin/login
         │
         ▼
┌─────────────────────────────┐
│  Fill Login Form            │
│  - Email                    │
│  - Password                 │
└────────────┬────────────────┘
             │
             ▼
  POST /api/admin/login
             │
             ▼
┌─────────────────────────────┐
│  Find Admin by Email        │
│  SELECT * FROM admins       │
│  WHERE email = $1           │
└────────────┬────────────────┘
             │
    ┌────────┴────────┐
    │                 │
  Found            Not Found
    │                 │
    ▼                 ▼
┌─────────────┐   ❌ Error
│ Compare     │   401 Unauthorized
│ Passwords   │
│ bcrypt.     │
│ compare()   │
└────┬────────┘
     │
 ┌───┴────┐
 │        │
Match   No Match
 │        │
 ▼        ▼
✅      ❌ Error
│       401 Unauthorized
│
▼
┌──────────────────────┐
│ Generate Token       │
│ random(32).hex()     │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Create Session       │
│ INSERT INTO          │
│ admin_sessions       │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│ Update Last Login    │
│ UPDATE admins        │
│ SET last_login       │
└──────────┬───────────┘
           │
           ▼
Return Token + Admin Info
           │
           ▼
localStorage.setItem('admin_token', token)
           │
           ▼
Redirect to /admin/dashboard ✅
```

### Settings Update Flow
```
Admin @ /admin/dashboard
       Settings Tab
         │
         ▼
┌──────────────────────────────┐
│  Fetch Current Settings      │
│  GET /api/admin/settings     │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Display Settings Form       │
│  - Populate with current     │
│    values from DB            │
└──────────┬───────────────────┘
           │
           ▼
Admin edits one or more fields
           │
           ▼
Admin clicks "Save Settings"
           │
           ▼
┌──────────────────────────────┐
│  Client Validation           │
│  - Check required fields     │
│  - Validate field formats    │
└──────────┬───────────────────┘
           │
           ▼
  PUT /api/admin/settings
  { store_name, store_email, ... }
           │
           ▼
┌──────────────────────────────┐
│  Server Validation           │
│  - Validate all inputs       │
│  - Check auth token          │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Update Database             │
│  FOR EACH setting:           │
│  UPDATE settings             │
│  SET value = $1              │
│  WHERE key = $2              │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Log Activity                │
│  INSERT INTO                 │
│  admin_activity_log          │
└──────────┬───────────────────┘
           │
           ▼
Return Success Message
           │
           ▼
Show "Settings saved successfully" ✅
```

## 📊 Database Relationship Diagram

```
ADMINS (superuser)
   │
   ├──► ADMIN_SESSIONS (many)
   │      └─ One admin has many sessions
   │      └─ 24-hour expiry
   │      └─ Token-based
   │
   ├──► ADMIN_ACTIVITY_LOG (many)
   │      └─ All actions logged
   │      └─ IP & user agent tracked
   │
   └──► SETTINGS (manages)
          └─ Admin updates settings
          └─ Updated_by field


CATEGORIES (6)
   │
   └──► PRODUCTS (many)
          ├─ One category has many products
          ├─ Cascading delete
          │
          └──► ORDER_ITEMS (many)
                 └─ Product appears in multiple orders


CUSTOMERS (many)
   │
   └──► ORDERS (many)
          ├─ One customer has multiple orders
          │
          └──► ORDER_ITEMS (many)
                 └─ Order contains multiple items
                 └─ Links to PRODUCTS
```

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────────────┐
│            AUTHENTICATION & AUTHORIZATION             │
└──────────────────────────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
   ┌────▼────┐    ┌────▼────┐   ┌────▼────┐
   │PASSWORD  │    │ TOKEN   │   │  ROLE   │
   │HASHING   │    │ SESSION │   │  BASED  │
   │          │    │         │   │ ACCESS  │
   │Bcrypt    │    │32-byte  │   │         │
   │10 rounds │    │hex      │   │Admin    │
   │          │    │24h exp. │   │Super    │
   │          │    │         │   │Admin    │
   └────┬─────┘    └────┬────┘   └────┬────┘
        │               │             │
        ▼               ▼             ▼
    ┌────────────────────────────────────┐
    │      DATABASE LAYER SECURITY       │
    ├────────────────────────────────────┤
    │ ✅ Parameterized Queries           │
    │ ✅ Foreign Key Constraints         │
    │ ✅ Activity Logging (IP tracking)  │
    │ ✅ Timestamps on all records       │
    │ ✅ Role-based record access        │
    └────────────────────────────────────┘
```

## 📈 Scalability Plan

```
PHASE 1 ✅ (Complete)
├─ Frontend branding
├─ Database schema
├─ Admin auth
└─ Settings CRUD

PHASE 2 (Ready)
├─ Products CRUD
├─ Categories CRUD
├─ Orders management
└─ Customers list

PHASE 3 (Future)
├─ Image uploads
├─ Inventory tracking
├─ Email notifications
└─ Analytics dashboard

PHASE 4 (Future)
├─ Payment gateway
├─ Frontend integration
├─ Shopping cart
└─ Customer accounts

PHASE 5 (Future)
├─ Multi-admin management
├─ Role permissions
├─ Advanced reporting
└─ Performance optimization
```

## 🚀 Deployment Ready

```
✅ DATABASE
   └─ Neon PostgreSQL configured
   └─ Migrations executed
   └─ All tables created

✅ ENVIRONMENT
   └─ DATABASE_URL set
   └─ Next.js build passing
   └─ Dependencies installed

✅ FRONTEND
   └─ All pages responsive
   └─ Images optimized
   └─ SEO metadata complete

✅ BACKEND
   └─ API routes secured
   └─ Error handling in place
   └─ Logging configured

✅ DOCUMENTATION
   └─ Setup guides complete
   └─ API docs ready
   └─ Database schema documented

READY FOR DEPLOYMENT ✅
```

---

**Architecture Design**: February 12, 2026
**Status**: Production Ready
**Next Phase**: Products & Orders CRUD Implementation
